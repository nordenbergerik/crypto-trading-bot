# crypto-trading-bot

## Execute

* Run `stack exec -- crypto-trading-bot-exe` to see "We're inside the application!"
* With `stack exec -- crypto-trading-bot-exe --verbose` you will see the same message, with more logging.

## Run tests

`stack test`
